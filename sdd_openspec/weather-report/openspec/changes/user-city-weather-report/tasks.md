## 1. 前端城市搜索与选择功能

- [ ] 1.1 创建城市搜索输入框与候选列表基础组件（含键盘与鼠标交互）
- [ ] 1.2 接入城市搜索 API（按城市名查询），实现输入节流与最小输入长度控制
- [ ] 1.3 在候选列表中展示城市名称 + 国家信息，并支持点击选择后写入当前选中城市状态
- [ ] 1.4 处理搜索无结果场景，在候选区域展示“未找到匹配城市”等友好提示

## 2. 天气报告展示与状态管理

- [ ] 2.1 基于选中城市实体（含城市 ID）封装天气查询方法（当前天气 + 可选短期预报）
- [ ] 2.2 在 UI 中实现天气报告展示区域（当前温度、天气描述、预报摘要）
- [ ] 2.3 为天气请求过程增加“加载中”状态展示（如骨架屏/Loading 指示），避免白屏
- [ ] 2.4 确保在天气数据未返回前不展示过期或错误的旧数据

## 3. 本地存储与默认城市恢复

- [ ] 3.1 设计并实现本地存储结构与键名（例如 `lastSelectedCity`），仅存储必要字段
- [ ] 3.2 在城市成功选中后，将城市关键信息序列化写入本地存储，并处理写入失败的降级逻辑
- [ ] 3.3 在页面初始化时读取本地存储中的最近城市信息，成功解析后自动触发天气查询
- [ ] 3.4 在本地数据缺失或解析失败时，回退到默认城市或空白初始状态，不向用户暴露底层异常细节

## 4. 错误与空状态处理及测试

- [ ] 4.1 统一定义城市搜索与天气查询的错误/空状态枚举与前端展示文案
- [ ] 4.2 实现错误状态面板组件，支持展示简要说明与“重试”按钮，并接入对应重试逻辑
- [ ] 4.3 为关键场景编写前端单元测试/集成测试（成功搜索、无结果、网络错误、本地存储不可用等）
- [ ] 4.4 对接 QA，基于 `user-city-weather-report` 的 Specs 场景清单完成手动验证回归

## 5. 3D 地球可视化与城市标记

- [ ] 5.1 选型并接入前端 3D 渲染方案（例如基于 WebGL 的轻量库），搭建基础地球球体渲染与交互骨架
- [ ] 5.2 设计城市到经纬度（或近似坐标）的映射策略，并实现根据城市数据计算地球表面标记位置的逻辑
- [ ] 5.3 在 3D 地球上实现城市高亮标记与视角联动（切换城市时更新标记位置并适当调整相机）
- [ ] 5.4 实现 3D 视图启用/禁用与降级逻辑，在不支持或高负载环境下关闭或简化 3D 渲染，确保核心天气功能不受影响

